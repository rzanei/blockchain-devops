## Set Up a Fast local Development Environment

## Solana Keygen
```bash
solana-keygen new --outfile /tmp/my-sol-wallet.json --no-passphrase
solana config set --keypair /tmp/my-sol-wallet.json
solana-test-validator
```
